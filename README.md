# pet-management-api
A lightweight TypeScript Express API for managing and querying pet data. Features route handling for retrieving pets by ID or query parameters (species, age, adoption status), custom input validation middleware for invalid IDs, and standard 404 error handling.
